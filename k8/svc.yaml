apiVersion: v1
kind: Service
metadata:
  name: meridian-service
  namespace: default

spec:
  type: NodePort

  selector:
    app: meridian

  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080